name: linting
on:
  workflow_dispatch:
jobs:
  ansible-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Code Checkout
        uses: actions/checkout@v4
      - name: List all Files
        run: ls
      - name: Ansible Linting
        uses: ansible/ansible-lint@main
